

# تحديد عدد الأسئلة الأكثر تكراراً بـ 8

تعديل بسيط في ملف `src/pages/AdminFAQ.tsx`:

- تغيير `limit_count: 50` إلى `limit_count: 8` في استدعاء `get_question_stats` (سطر 43)

هذا سيجعل الصفحة تعرض 8 أسئلة فقط كحد أقصى بدلاً من 50.

