

# تغيير رقم دخول المشرف إلى 20260000

## الوضع الحالي
المشرف يدخل بكتابة "admin" في حقل الرقم الجامعي + كلمة المرور. نريد تغييره إلى "20260000".

## التعديلات

### 1. `src/pages/Login.tsx` — سطر 46
تغيير شرط التحقق من المشرف:
```typescript
// من
if (studentId.trim() === "admin") {
// إلى  
if (studentId.trim() === "20260000") {
```

### 2. `src/components/AdminLayout.tsx` — التحقق من الجلسة
مراجعة أي مكان يتحقق من بيانات المشرف في localStorage للتأكد من التوافق.

تعديل بسيط في ملف واحد أساسي.

